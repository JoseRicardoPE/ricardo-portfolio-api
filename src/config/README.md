# Config

Contains application and infrastructure configuration.

Examples:

- Environment configuration
- Database configuration
- Server configuration
- External service configuration

Configuration should be centralized and must not expose secrets or sensitive values.