# Shared

Contains reusable backend code that is not specific to a single business module.

Examples:

- Shared types
- Utilities
- Common constants
- Reusable errors
- Generic helpers

Code should only be moved to `shared/` when it is genuinely reusable across multiple modules.